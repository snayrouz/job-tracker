Title: <%= @job.title %>
<br>

Description: <%= @job.description %>
<br>

Level of Interest: <%= @job.level_of_interest %>
<br>

City: <%= @job.city %>
<br>

Company: <%= link_to @job.company.name, company_path(@job.company) %>
<br>


<h3>Comments (<%= @job.comments.size %>)</h3>
<%= render partial: 'jobs/comment', collection: @job.comments.reverse %>
<%= render partial: 'comments/form' %><br>

<%= link_to "Edit Job", edit_company_job_path(@job.company) %>
<br>

<%= link_to "Delete Job", company_job_path(@job.company, @job), method: :delete, data: { confirm: 'Are you sure you want to delete this job?' } %>
